	<ul>
		<% @my_events.each do |e| %>
		<li>
			<img src="<%= e.flyer.url(:small) %>"/>
			<%= e.name %> <%= link_to "Edit", edit_event_path(e), :remote => true %> <%= link_to "Destroy", e, :remote=>true, :confirm=>'Are you sure?', :method => :delete %>
		</li>
		<% end %>
	</ul>