{
	"events":{
	<% i = 1 %>
	<% n = @search_results.count %>
	<% @search_results.each do |result| %>
		"event<%=i%>":{ "id": "<%=result.id%>","name" : "<%=result.name %>", "description" : "<%=result.description%>" }<%="," unless i == n%>
		<% i=i+1%>
	<% end %>
	}
}